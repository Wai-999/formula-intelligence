# Build Log — ML Mode

One entry per module: what was built, files touched, decisions made and why, verification results.

---

## Phase 0: Discovery

**Stack:** React 19 + Vite 8, plain JavaScript (JSX, no TypeScript — `@types/react*` devDependencies are unused Vite-template vestiges). Package manager: npm (`package-lock.json` present).

**Router:** None. No `react-router-dom` or equivalent in `package.json`. Navigation is 100% client-state: `useUIStore.activeTab` selects which tab renders; `AppShell.jsx` keep-alive-mounts every tab's page component simultaneously and toggles `display: flex|none` — this is why switching tabs never loses state (scroll position, in-progress form input, D3 zoom/pan). There is no URL sync today (no hash routing, no query params) for any of the 9 existing tabs.

**Existing "Stats mode" implementation** (this *is* the whole app today — there is no existing multi-mode concept to find):
- Entry: `src/App.jsx` → `src/components/layout/AppShell.jsx` (renders header + `IconRail` + the active tab's page, all keep-alive mounted).
- Nav: `src/components/layout/IconRail.jsx`, driven by the `TABS` array exported from `src/store/useUIStore.js`.
- The graph/map itself: `src/components/graph/GraphCanvas.jsx` (D3 v7: force-free hierarchical layout via `src/components/graph/hierarchicalLayout.js`, zoom/pan via `d3.zoom()`, node select/drag via `d3.drag()`) + `src/components/detail/DetailPanel.jsx` (slide-in node detail).
- Data/schema: `src/data/nodes.js` (94 formula nodes: `{id, ch, name, short, formula, desc, use, tags}`), `src/data/links.js` (117 edges: `{s, t, type}` where type ∈ prereq/extends/applies/nonparam/family), `src/data/chapters.js` (13 chapters, each with an id/name/color — this *is* the app's existing color-coding-by-category convention). Plus `bridgeText.js` (real-world usage), `inventors.js`, `prereqChains.js`, `storylines.js`, `practiceProblems.js` — all keyed by node id.
- `hierarchicalLayout.js` is already a **pure, generic function**: `computeHierarchicalLayout(nodes, links) → {positions, chapterRowY, bounds}`. It only assumes `node.ch` (a row-grouping key) and `link.s/t` (source/target ids) — nothing Stats-specific. This is directly reusable for the ML model graph (Module 3) by treating "model family" as the row-grouping key instead of "chapter."
- `GraphCanvas.jsx` itself is **not** generic — it hard-imports `nodes, links, CHAPTERS, chapterColorMap` from `../../data/index.js` at module scope (not via props), and its click handler, `DetailPanel` wiring, and `useMasteryStore` integration all assume the Stats node shape (`formula`, `desc`, `use`, prereq-chain tracing). See Phase 1 §1 for the resulting sibling-component decision.

**State management:** Zustand (`^5.0.14`), one store per concern (`useUIStore`, `useMasteryStore`, `useSRStore`, `usePracticeStore`, `useJournalStore`, `useLearningPathStore`, `useStoryWalkStore`, `useErrorLogStore`, `useToastStore`), each a flat `create((set, get) => ({...}))` object with state + actions co-located, most persisting to `localStorage` via `src/lib/storage.js` (`loadJSON`/`saveJSON`) keyed through `src/data/storageKeys.js`.

**Styling:** Plain CSS, one `.css` file per component (co-located, imported directly in the `.jsx` file — no CSS Modules, no Tailwind, no styled-components). Design tokens live as CSS custom properties in `src/index.css` `:root`: `--primary: #8b5cf6` (violet, the app's core identity color), `--primary-2: #6366f1`, `--accent: #22d3ee` (cyan, already used for the graph's "Extends" edge type and elsewhere as the secondary/technical accent), `--success/--warning/--danger`, `--surface/--surface-2/--surface-glass`, `--border/--border-strong`, `--text-primary/--text-muted/--text-faint`, `--radius-sm/md/lg`, `--font-sans/--font-mono`. Dark-only (`color-scheme: dark`, no light theme).

**Charting/visualization:** D3 v7 (`d3`) for the graph; `framer-motion` for transitions (used sparingly today — e.g. toasts). No KaTeX/MathJax — formulas are hand-typeset Unicode strings (e.g. `"X̄ = ΣX / n"`), not rendered LaTeX. ML mode will follow the same convention for consistency rather than introducing a math-typesetting dependency.

**i18n/localization:** None exists today — the app is English-only, and there is no beginner/researcher depth toggle anywhere in Stats mode. Module 11's bilingual + audience-depth layer will be genuinely new infrastructure, built as a small shared utility (`src/lib/mlContent.js`) rather than adopting a heavyweight i18n library, consistent with rule 5 (prefer no new dependency).

**Verification commands:** `npm run build` (`vite build`), `npm run lint` (`oxlint`), `npm run dev` / `npm run preview`. No `typecheck` script (no TypeScript in this repo) and no `test` script exist — neither will be added; `npm run build` + `npm run lint` are this repo's real ceiling for automated verification, exactly as rule 7 anticipates ("if `typecheck`/lint-equivalent don't exist... note it"). Confirmed both pass cleanly on the pre-ML-mode codebase (0 errors, 0 lint warnings) before starting.

**Local run:** Confirmed working via `npm run dev` (port 5174, per `.claude/launch.json` at the parent-folder level) and `npm run preview` (port 5175) earlier this session.

**Decision:** Per the prompt's own fallback clause, since there is no separate "Stats page" to *find* (the whole app *is* Stats mode), the plan is: promote the existing experience to live under an explicit `mode: 'stats'` state, add a sibling `mode: 'ml'` experience, and add a persistent mode switcher in `app-header`. This is a light, additive change to `AppShell.jsx`/`useUIStore.js`, not a rewrite.

---

## Phase 1: Architecture

**1. Shared rendering engine, separate content.** `hierarchicalLayout.js` is reused as-is for the ML model graph (it's already generic). `GraphCanvas.jsx` is **not** parameterized in place — it's tightly coupled to the Stats node shape and to `useMasteryStore`/`DetailPanel` in ways that would risk regressing Stats mode for uncertain benefit. Per the prompt's explicit escape hatch ("acceptable to build a sibling component that visually and behaviorally matches it — document why"), Module 3 builds `src/components/mlgraph/MLRelationshipMap.jsx`: same interaction paradigm (D3 pan/zoom/click-to-expand, same hierarchical layout algorithm, same visual language/CSS tokens), separate component, separate detail panel tailored to the model-card shape (`howItWorks/advantages/weaknesses/usageAreas/compass`). Both graphs are thin D3-in-React wrappers around the *same* `computeHierarchicalLayout` function, so this is "share the engine, not the component" rather than a fork.

**2. Routing.** No router is installed and the existing 9 Stats tabs have no URL sync at all today — adding a router *only* for ML mode would be an inconsistent, foreign pattern, and rule 5 says prefer no new dependency. Decision: extend the *existing* convention (Zustand-driven `activeTab`/keep-alive mount) with a new top-level `mode: 'stats' | 'ml'` field, **plus** a small dependency-free hash-sync utility (`src/lib/hashRoute.js`) that mirrors `mode` + the active tab into `location.hash` (e.g. `#ml/pipeline`, `#stats/map`) and restores it on load/`hashchange`. This delivers the spec's actual goal — ML mode is linkable/bookmarkable/shareable — without adding a router dependency or touching Stats mode's existing lack of URL sync beyond opting it into the same hash format. ML feature pages are also `React.lazy`-loaded so Stats mode's bundle doesn't grow.

**3. State.** `useUIStore.mode` + `useUIStore.mlActiveTab` (mirroring `activeTab`, so each mode remembers its own last-open tab across a switch — "preserve where it makes sense"). `selectedNodeId`/`traceChain` reset on mode switch (they're Stats-graph-specific and don't map onto the ML graph's different id space — "reset gracefully where it doesn't"). New ML-only stores, one per concern, mirroring the existing one-store-per-feature convention: `useMLUIStore` (selected ML node, beginner/researcher level, EN/MY language — cross-cutting, lives at the ML-shell level), `useMLPlaygroundStore`, `useMLDomainStore` (driver-panel + scenario-preset state, shared shape reused by Gold/Macro/Micro/Politics so it's one store, not four). Cross-mode concept (Module 10's bridge, Module 9's geo-risk↔Gold cross-link) uses a tiny shared slice on `useUIStore`: `linkedConceptId` + `navigateToLinkedConcept()`.

**4. Data strategy.** Every figure/finding in `ML-Research-Reference.md` (now at `docs/research/ML-Research-Reference.md`) is treated as the real, citable content. Full historical series (e.g. a 5-year gold price line) don't exist in the doc as a full series — those are generated as synthetic-but-realistic series calibrated to the doc's stylized facts (current $4,000–$4,030/oz level, the doc's stated drivers' directional relationships, GARCH-style volatility clustering) and visibly labeled in-UI as illustrative. `docs/DATA_SOURCES.md` tracks real vs. synthetic per module, per Module 12.

**5. Design language.** No new accent color invented. ML mode uses the *existing* `--accent` (cyan `#22d3ee`) as its mode-identity color (Stats mode's implicit identity is `--primary` violet, used for its header dot/active-tab states) — cyan is already in the palette and already carries a "technical/data" association in this app (the graph's "Extends" edge type, several chapter colors). New CSS custom properties are added only for concepts that don't exist yet (e.g. `--ml-edge-competes`, `--ml-edge-combines` for the new edge-type colors in Module 3) and are placed in `:root` alongside the existing tokens, not hard-coded per-component.

---

## Module 1: Shared Shell & Mode Switcher

**Built:** `Stats | ML` segmented toggle in the app header; `mode`/`mlActiveTab`/`linkedConcept` state on `useUIStore`; a new `useMLUIStore` (Beginner/Researcher level, EN/MY language, selected model id — persisted to `localStorage` under `ml_mode_prefs_v1`, added to the central `STORAGE_KEYS` registry); `src/lib/mlContent.js` (`bl()`/`useT()` bilingual-content helper, built now rather than deferred to Module 11 since every module from here on needs it — see rule 1 rationale below); `src/lib/hashRoute.js` (dependency-free `#mode/tab` sync, restores on load, updates on navigation); `MLIconRail`/`MLBody`/`ModeSwitcher`/`LevelLangToggle` components; `StatsBody.jsx` (Stats mode's existing IconRail+tabs markup extracted verbatim, zero behavior change); 9 lazy-loaded placeholder pages under `src/features/ml/*` (one per upcoming module, swapped for real content as each module is built).

**Files touched:** `src/store/useUIStore.js` (added mode state), `src/store/useMLUIStore.js` (new), `src/data/storageKeys.js` (added `mlModePrefs` key), `src/lib/mlContent.js` (new), `src/lib/hashRoute.js` (new), `src/components/layout/{AppShell,StatsBody,MLBody,MLIconRail,ModeSwitcher}.jsx` + matching `.css`, `src/components/ml/{LevelLangToggle,MLPlaceholder}.jsx` + `.css`, `src/features/ml/*/{Pipeline,ModelMap,Playground,Evaluation,GoldLab,MacroLab,MicroLab,PoliticsLab,Bridge}Page.jsx` (9 placeholders).

**Decisions (rule 1 — logged, not asked):**
- Built the bilingual/level content helper (`mlContent.js`) now instead of waiting for Module 11, since retrofitting it into 9 already-written modules would mean touching every module twice. Module 11 becomes "verify every module actually uses it correctly" rather than "add it."
- ML tab icons picked from tabler-icons' common/stable glyph set (`ti-route`, `ti-share-2`, `ti-coin`, etc.) without a dependency check against the exact pinned CDN version — visually confirmed all render correctly in-browser, so no further action needed.
- `MLIconRail` imports `IconRail.css` directly rather than duplicating its rules, so the two rails can never visually drift apart.

**Verification:**
- `npm run build`: pass (665 modules, each ML feature page confirmed in its own lazy chunk — code-splitting working).
- `npm run lint` (oxlint): pass, 0 warnings.
- Visual smoke test: mode switch both directions confirmed instant; Stats mode confirmed pixel-identical to pre-Module-1 (94 nodes, 13 chapters, all sidebar content, graph rendering); hash sync confirmed round-trip (`#ml/gold` → reload → restores ML mode on the Gold tab); all 9 ML tabs reachable and each shows its own distinct placeholder (verified scoped to the visible keep-alive pane — the accessibility tree/DOM contains all 9 simultaneously, so an unscoped query returns the first, not the visible, one — a known false-positive trap from this session's earlier Stats-mode testing).
- Responsive check at 360/768/1280/1920px: found and fixed a real header overlap at ≤560px (title/subtitle wrapping to multiple lines and colliding with the new mode switcher) — this pre-dated Module 1 in Stats mode's header, but since Module 1 is what added a competing element to that same header, fixed it there (subtitle now truncates with ellipsis and hides entirely under 560px; ML tab header now wraps onto two rows instead of overlapping). No other Stats-mode layout was touched.
- Console: 0 errors/warnings across every check above.

---

## Module 2: ML Pipeline Visualizer

**Built:** `src/data/ml/pipeline.js` — all 7 stages from the research doc §1 table, each with a question/what-happens pair plus a dedicated Gold-price worked example, fully bilingual/two-depth via `bl()`. `PipelineVisualizer.jsx` — a horizontally-scrollable 7-node stepper (numbered circles + connectors), click any stage to expand its detail below (accordion, one open at a time). `src/data/ml/estimationPredictionCausal.js` — the full §2 table (Estimation/Prediction/Causal Inference × Goal/Output/Example/Methods/Failure mode), bilingual/two-depth. `EstimationPredictionCausal.jsx` — the 3-tab interactive panel; each tab, beyond the table content, renders a concrete demo: **Estimation** shows a coefficient+CI card and a working button that jumps straight into Module 10's Bridge tab (`useUIStore.navigateToLinkedConcept`); **Prediction** shows a live point-forecast+interval bar (`PredictionIntervalDemo.jsx`); **Causal Inference** shows an SVG confounder diagram — DXY predicting gold well without causing it, both driven by Fed policy (`ConfounderDemo.jsx`). `src/features/ml/mlPageShared.css` — the shared page-chrome conventions (`.ml-page`, `.ml-section`, `.ml-lbl`, etc.) every subsequent module reuses. `MLCitation.jsx` — the citation/synthetic-data-label component mandated by Section F.5, used here and reused everywhere from now on.

**Files touched:** `src/data/ml/{pipeline,estimationPredictionCausal}.js` (new), `src/features/ml/pipeline/{PipelinePage,PipelineVisualizer,EstimationPredictionCausal,EstimationDemo,PredictionIntervalDemo,ConfounderDemo}.jsx` + matching `.css` (new; `PipelinePage.jsx` replaces its Module 1 placeholder), `src/features/ml/mlPageShared.css` (new), `src/components/ml/MLCitation.jsx` + `.css` (new).

**Decisions (rule 1):**
- Caught and fixed a real Rules-of-Hooks violation while writing this module: `useT()` was initially called inside an `EPC_COLUMNS.map()` callback in the parent component. Extracted a small `EPCTabButton` subcomponent so the hook call happens at a stable component top-level instead — the correct React pattern, not just a lint-silencer.
- The pipeline stepper is a horizontally-scrolling row rather than a fixed 7-column grid, so it degrades to "swipe sideways" at 360px instead of needing per-breakpoint label truncation — the same resilience-over-cleverness choice as the Stats map's own responsive handling.
- `--danger` (existing token) reused for the confounder diagram's "predicts well but doesn't cause" arrow and for the EPC "failure mode" callout box — no new red was invented.

**Verification:**
- `npm run build`: pass; `PipelinePage` confirmed in its own ~46KB lazy chunk (its content is now real, not a placeholder).
- `npm run lint`: pass, 0 warnings (including after the Rules-of-Hooks fix above).
- Visual smoke test: all 7 pipeline stages open correctly with distinct content; all 3 EPC tabs verified with their demos (coefficient card, interval bar reading exactly $4,030–$4,330 around a $4,180 point per the $4,180±$150 example text, confounder SVG diagram); Beginner→Researcher toggle confirmed changing displayed text; EN→Burmese toggle confirmed changing displayed text (spot-checked the framing-stage question and researcher-depth body text, both matched the authored Burmese exactly); Burmese glyphs render cleanly (no tofu boxes) under the existing `--font-sans` stack; Estimation→Bridge cross-link button confirmed landing on Module 10's tab (`Stats ↔ ML` both in the header and the active icon-rail button).
- Encountered and diagnosed two false alarms during testing, both testing-harness artifacts rather than app bugs (logged here so future verification passes don't waste time rediscovering them): (1) an HMR-triggered full reload during active file editing transiently reset mode/tab state — resolved by a clean re-navigation, not a code fix; (2) rapid chained click scripts without settling time between them produced confusing intermediate reads — resolved by spacing interactions across separate tool calls, consistent with this session's established testing pattern.
- Console: 0 errors across the entire session, including through both false-alarm investigations.

---

## Module 3: ML Model Relationship Map

**Built:** `src/data/ml/models.js` — all 31 rows from the research doc's §3 tables as 32 nodes (RL split into Q-Learning/Actor-Critic, see decisions below), each with a genuinely bl()-forked `howItWorks` (beginner analogy vs. researcher notation) plus doc-sourced `advantages`/`weaknesses`/`usageAreas` (bilingual via the new `blSame()` helper) and a `compass: {interpretability, dataHunger, nonlinearity}` 1–5 rating per the doc's §4 Model Selection Compass. `ML_LINKS` — every edge named in the build spec's edge-structure list, typed `extends`/`competes`/`combines`. `src/components/mlgraph/MLRelationshipMap.jsx` — the sibling graph component (Phase 1 §1's planned path), reusing `computeHierarchicalLayout` and the exact click-detection approach proven in Stats mode's `GraphCanvas.jsx` (d3.drag/d3.zoom's own start/end gesture, not hand-rolled mousedown/mouseup — ported directly, comments included, so the "why" travels with the code). `MLModelDetailPanel.jsx` — the per-node detail view (family badge, how it works, advantages/weaknesses in color-coded callouts, usage areas, `CompassMeter.jsx`'s three filled-bar meters, and a clickable connections list that jumps between related nodes). `ModelMapPage.jsx` — ties it together with a legend matching Stats mode's own legend convention.

**Files touched:** `src/data/ml/models.js` (new, ~32 nodes/edges), `src/components/mlgraph/{MLRelationshipMap,MLModelDetailPanel}.jsx` + `.css` (new), `src/components/ml/{CompassMeter}.jsx` + `.css` (new), `src/features/ml/modelmap/ModelMapPage.jsx` + `.css` (replaces its Module 1 placeholder), `src/lib/mlContent.js` (added `blSame()`), `src/components/graph/hierarchicalLayout.js` (extended with optional `{rowSpacing, columnSpacing}`, defaults unchanged — see decisions).

**Decisions (rule 1):**
- **Adapted several edges to the doc's actual row structure**, since the build spec's edge list names a few sub-concepts that aren't their own doc table row: "Bayesian Linear Regression" isn't a row (only "Gaussian Processes" and "Bayesian Structural Time Series / Bayesian NNs" are, under §3.9) — its two named edges were re-anchored to `Linear Regression`, which is the closest real node and is genuinely what becomes Bayesian in that lineage. "Q-Learning" and "Actor-Critic" are one merged row in the doc ("RL (Q-learning, Actor-Critic, etc.)") — split into two nodes sharing the doc's own (undifferentiated) advantages/weaknesses/usage text, specifically so the spec's named "policy-gradient evolution" edge between them could exist at all; this is the one place a node was added beyond a literal one-per-row reading, done because the alternative was silently dropping a named edge.
- **PCA's "feeds into almost every other node" was scoped down** to three representative combines-edges (→ Bayesian Structural Time Series, → Random Forest, → LSTM/GRU) rather than a literal 31-edge fan-out, which the module's own "no label overlap" requirement would make unworkable. The doc's own usage-area text for PCA specifically calls out the DFM/factor-model pairing, so that one is exact; the other two stand in for "and many more" without cluttering the graph.
- **Found and fixed a real overlap bug during verification** (not a decision made in advance): the shared `computeHierarchicalLayout`'s fixed 68px column spacing, tuned for Stats mode's short formula names, produced 14 overlapping label pairs for ML's much longer model names ("Ridge / Lasso / Elastic Net", "Gradient Boosting (general)"). Fixed by extending the shared function with optional `rowSpacing`/`columnSpacing` parameters (defaulting to the original hard-coded constants, so Stats mode's call site is byte-for-byte unaffected) rather than forking the layout function or hard-coding a second constant — this is the "extend the shared engine" path Phase 1 §1 called for, applied for real once a real need showed up.
- Model-family row colors (`ML_FAMILIES`) picked from hues adjacent to but distinct from the existing `--primary`/`--accent` tokens, following the same "varied-but-harmonious" spread Stats mode uses for its 13 chapter colors — not matched 1:1 to any existing token since there was no natural 10-way mapping, but every color is a small, deliberate variation on the existing dark-theme palette rather than an arbitrary addition.

**Verification:**
- `npm run build`: pass; `ModelMapPage` in its own ~87KB lazy chunk.
- `npm run lint`: pass, 0 warnings (one caught and fixed: an unused `style` destructure in the marker-color loop, resolved by actually using `style.color` instead of hard-coding three duplicate hex values — better code, not just a silenced warning).
- Label-overlap check (Section G's explicit requirement) done programmatically, not just eyeballed: a script measuring pairwise bounding-box intersection of all 32 `.mlgnode-label` elements, run fresh at each of 360/768/1280/1920px. Result: 0 overlaps at all four widths after the spacing fix (14 overlaps before it, confirming the fix actually mattered rather than just looking fine).
- Interaction smoke test: single-click open confirmed on a model in the middle of the graph (XGBoost) with correct neighborhood highlighting (its 5 connected nodes stay full-opacity, everything else dims); background click-to-deselect confirmed (with an `elementFromPoint` sanity check first, after one throwaway coordinate landed on a node instead of background — a testing-script mistake, not an app issue); clicking a connection in the detail panel (XGBoost → Prophet) correctly navigated the panel to Prophet's own card.
- Cross-check against the research doc: spot-verified XGBoost's and Prophet's rendered advantages/weaknesses text against doc §3.3/§3.7 — verbatim in substance, lightly reworded for the bullet format.
- Re-verified Stats mode after touching the shared `hierarchicalLayout.js`: reloaded, switched to Stats, confirmed the graph renders pixel-identical to pre-Module-3 (94 nodes, 13 chapters, same layout) — the optional-parameter extension is confirmed non-breaking, not just assumed so.
- Console: 0 errors throughout.

---

## Module 4: Model Playground (bias-variance sandbox)

**Built:** `src/lib/mlSandbox.js` — genuine client-side model fitting with no new dependency: a seeded PRNG (Mulberry32 + Box-Muller) for a reproducible synthetic dataset around a moderately wiggly true function, a polynomial-regression fitter (normal equations solved via Gaussian elimination with partial pivoting), and a depth-limited regression-tree fitter (recursive variance-reducing binary splits) — plus `complexityCurve()`, which fits the *entire* complexity range up front so the error-vs-complexity chart doesn't need to refit on every slider tick. `PlaygroundPage.jsx` — model toggle (Polynomial / Decision Tree, each with its own complexity semantics and range), noise slider, complexity slider, a "New sample" reseed button, `ScatterFitChart.jsx` (D3-scale-driven SVG: training/validation points, the fitted curve, and the true function for comparison), `ErrorCurveChart.jsx` (train/validation MSE across the full complexity range with a marker at the current position and a dashed line at the validation-optimal complexity), and a zone banner that classifies the current complexity as under/good/over-fit relative to that optimum and explains why, bilingual/two-depth via `src/data/ml/playground.js`.

**Files touched:** `src/lib/mlSandbox.js` (new), `src/data/ml/playground.js` (new), `src/features/ml/playground/{PlaygroundPage,ScatterFitChart,ErrorCurveChart}.jsx` + `.css` (new; replaces the Module 1 placeholder).

**Decisions (rule 1):**
- Two fittable models (not one) because the spec's "polynomial degree or tree depth depending on model chosen" phrasing implies model choice is part of the module, and having two makes the underlying concept (complexity trades bias for variance regardless of *which* knob controls it) land more clearly than one model alone would.
- The error-vs-complexity curve is precomputed across the *entire* range on every dataset/model change, not just at the current slider value — this is what makes the U-shaped validation curve visible as a whole shape (with a "you are here" marker) rather than requiring the learner to mentally accumulate points by dragging back and forth.
- Charts are hand-rolled SVG using D3's scale/line generators for the math only (no D3 DOM manipulation, no new charting library) — consistent with rule 5 and with how the rest of the app already uses D3 (GraphCanvas/MLRelationshipMap use it for real DOM work; this is the "D3 for math, React for rendering" half of the library the app already depends on).

**Verification:**
- `npm run build`: pass; `PlaygroundPage` in its own ~29KB lazy chunk.
- `npm run lint`: pass, 0 warnings (one caught and fixed: an unused `n` variable left over from an earlier draft of `fitPolynomial`).
- Interaction smoke test, the module's actual done-criterion: complexity slider driven to its minimum (1) → "Underfitting" banner, train MSE 2.486, validation MSE 3.869 (close together, both high, exactly the underfitting signature); driven to its maximum (12) → "Overfitting" banner, train MSE 0.299, validation MSE 0.902 (train kept falling, validation rose back up — the diverging signature), and the fitted curve visibly bent itself around individual noisy points instead of the smooth true function. Switching to Decision Tree re-rendered a visibly distinct step-function fit (as opposed to the polynomial's smooth curve) with its own depth-based complexity range.
- One transient false alarm during this module's testing: a single check immediately after a rapid slider-set script reported the app back in Stats mode with no ML DOM present at all. Re-run cleanly (fresh navigation, deliberate single actions per tool call) reproduced correctly every time afterward, including the exact same slider-to-12 action that had appeared to fail — treated as the same class of harness/HMR flakiness logged in Modules 2 and 3, not a real defect, since it could not be reproduced under controlled conditions and no console error accompanied it either time.
- Console: 0 errors across every check, including through the false alarm.

---

## Module 5: Evaluation & Explainability Lab

**Built:** Four sub-panels on one page, per the research doc §5. `MetricsPanel.jsx` — MAE/RMSE/MAPE/R²/AIC/BIC computed live against a model/complexity choice, reusing Module 4's `fitPolynomial`/`fitTree` directly (added `computeMetrics()` to `mlSandbox.js` rather than a separate metrics path, so "the same fits" is literal, not just similar). `BacktestAnimator.jsx` — two timelines over the same 30-step series: a fixed naive random 70/30 split (deliberately time-order-blind, so train cells visibly land after test cells) versus an animated walk-forward window with Play/Pause/scrub/Reset, sliding the train/test boundary forward over time. `ShapLimePanel.jsx` — a static SHAP-style global importance bar chart (5 features, matching Module 6's gold drivers so this previews that lab rather than using an unrelated example) and a static LIME-style waterfall for one prediction (base value → per-feature push → final prediction), both clearly labeled synthetic per Section C.4 since no real SHAP/LIME library was run (Module 5's own spec explicitly permits this substitution). `ConceptDriftDemo.jsx` — a model trained only on the pre-break half of a series whose generating relationship's slope flips sign partway through; a scrubbable rolling-error chart against a retrain threshold, with a live "Drift detected — retrain?" flag once enough post-break error accumulates.

**Files touched:** `src/lib/mlSandbox.js` (added `computeMetrics`, `generateDriftSeries`), `src/data/ml/evaluation.js` (new), `src/features/ml/evaluation/{EvaluationPage,MetricsPanel,BacktestAnimator,ShapLimePanel,ConceptDriftDemo}.jsx` + `.css` (new; replaces the Module 1 placeholder).

**Decisions (rule 1):**
- SHAP feature set deliberately matches Module 6's gold drivers (real yields/DXY/geopolitical risk/central-bank demand/inflation) rather than a generic example, so this module previews the flagship domain lab instead of teaching the concept in isolation — consistent with the research doc's own note that Gold should be the connective thread.
- The naive-split row uses a *fixed* pre-computed random assignment (seeded, not re-randomized on every render) so the "train appears after test" leakage is a stable, inspectable fact of that specific split rather than a flickering coincidence — deliberately not disguising how obviously wrong it looks once you see it laid out on a timeline.
- AIC/BIC use the standard Gaussian-likelihood RSS approximation (`n·ln(RSS/n) + k·penalty`) rather than a full likelihood computation per model family, since the playground's fitters don't produce a proper likelihood — noted here rather than silently presenting it as more rigorous than it is; still directionally correct and comparable across complexities, which is what the module needs it for.

**Verification:**
- `npm run build`: pass; `mlSandbox.js` split into its own shared chunk once a second page (`EvaluationPage`) started importing it alongside `PlaygroundPage` — confirms the "share the fitting code" decision actually took effect at the bundler level, not just in source.
- `npm run lint`: pass, 0 warnings.
- Interaction smoke test: all four sub-panels render with correct content on load; metrics table updates when switching model/complexity; walk-forward Play button confirmed genuinely animating (origin advanced from t=8 to t=12 over ~2s, matching the 550ms step interval); concept-drift scrubber driven to its maximum correctly flipped the flag from "tracking within normal range" to "Drift detected — retrain?", cross-checked against the raw rolling-error numbers (0.3–0.6 pre-break, jumping to 9.5→32 immediately post-break) to confirm the threshold crossing is a real signal, not a coincidence of the demo data.
- Two testing false alarms, both resolved and neither a real defect: (1) reusing the same browser tab across `preview_stop`/`preview_start` for a new module showed stale content from the previous module until an explicit hard `navigate()` — noting this as a standing procedure for the remaining modules rather than re-discovering it each time; (2) a slider-drag script using `scrub.max` as the target value silently failed to stick on the first attempt (reverted to its default) but succeeded immediately on a second, identical attempt with the value hard-coded instead of read from the DOM in the same expression — logged as a timing quirk in stale-element references, not reproduced consistently enough to chase further.
- Console: 0 errors throughout, including through both false alarms.

---

## Module 6: Gold Price Forecasting Lab

**Built:** The reusable domain-lab architecture Phase 1 anticipated for Modules 6-9, plus Gold as its first real instance. `src/lib/domainModel.js` — `computeModelForecasts()`: one shared driver→price math core producing four qualitatively distinct "lenses" over the identical driver state, matching the research doc §3's comparative findings rather than four independent formulas: econometric (ARIMA-like, damped ×0.7, band widens once any driver exceeds ~1 SD), deep-learning (LSTM-like, full linear response amplified ×(1+0.12·(alignedCount−1)) when drivers point the same way), tree-ensemble (XGBoost-like, per-driver `tanh` saturation), and volatility (GARCH-like, point forecast tracks the econometric lens, band width instead responds to driver magnitude and geopolitical risk specifically). `driverContributions()` — the per-driver dollar decomposition for the trace panel. `src/data/ml/domains/gold.js` — the doc's real §6.1 figures (spot ~$4,000–$4,030/oz, 2026 bank targets $4,800–$6,500/oz with named banks) as the only hard numbers; the five drivers' *directions* are doc-sourced, their coefficient *magnitudes* are labeled illustrative in the file's own header comment. `src/components/ml/domain/{DriverPanel,ScenarioPresets,ForecastBandChart,TracePanel}.jsx` — the four generic building blocks every domain lab (Gold now, Macro/Micro/Politics next) will reuse as-is: a slider grid, scenario-preset buttons that overwrite the whole driver state at once, a D3-scaled SVG band chart comparing all four models, and a "why did this move?" bar decomposition. `useMLDomainStore.js` — one Zustand store keyed by domain id (`gold`/`macro`/`micro`/`politics`) rather than four separate stores, since the driver-state shape is identical across labs. `GoldLabPage.jsx` ties these together with the context card, drivers, chart, and trace panel.

**Files touched:** `src/lib/domainModel.js`, `src/store/useMLDomainStore.js`, `src/data/ml/domains/gold.js`, `src/components/ml/domain/{DriverPanel,ScenarioPresets,ForecastBandChart,TracePanel}.jsx` + `.css` (new), `src/features/ml/domainLabShared.css` (new), `src/features/ml/gold/GoldLabPage.jsx` (new; replaces the Module 1 placeholder).

**Decisions (rule 1):**
- One shared `computeModelForecasts()` function rather than four per-model implementations, so Macro/Micro/Politics inherit the same four-lenses-diverge-on-identical-input behavior for free — a domain module becomes "content" (drivers, coefficients, scenario presets) exactly as Phase 1 predicted, not new mechanics.
- LSTM's "amplified when drivers align" and XGBoost's "saturates instead of growing without limit" are deliberately simple stand-ins for each real algorithm's documented qualitative behavior (research doc §3), not real fits — this is called out explicitly in `domainModel.js`'s own header comment rather than left implicit, consistent with the anti-fabrication rule.
- **Found and fixed a real mobile bug during verification** (not a decision made in advance): `ForecastBandChart`'s SVG has a legibility-driven `min-width: 480px` (at its 640-unit viewBox, anything narrower renders sub-4px text) wrapped in `overflow-x: auto`, which is the correct pattern per this project's design conventions for wide content — but at 360px the content column is only ~214px wide, so more than half the chart, including every model's dollar point-label, was scrollable but invisible with zero visual affordance. Fixed by adding a small `canScrollRight`-driven chevron hint to the shared component (not just Gold's instance), so the fix benefits Macro/Micro/Politics too. Getting the hint to actually appear reliably surfaced a second, genuine timing bug: a plain mount-time `useEffect` measurement can race Vite's dev-mode CSS injection and read the container before `min-width` has taken effect; switched to `useLayoutEffect` (synchronous, post-commit) plus a 150ms settle-timer re-check, with `ResizeObserver` and a `window.resize` listener keeping it correct afterward. Verified against real pixel measurements at each step (not just visually) before accepting the fix.

**Verification:**
- `npm run build`: pass; `GoldLabPage` in its own ~17KB lazy chunk, `linear`/`mlSandbox` chunks shared correctly.
- `npm run lint`: pass, 0 warnings.
- Interaction smoke test against the module's actual done-criterion ("move any one driver, see all four models react differently"): moved Geopolitical risk to +1.8 → ARIMA +139 (damped), LSTM +198 (full linear, exactly matches the trace panel's own +$198 for that driver), XGBoost +113 (saturated, smallest move), GARCH +139 (tracks ARIMA) — all four numbers hand-verified against `domainModel.js`'s formulas, not just eyeballed. Applied the "Dollar strengthens sharply" scenario preset → all 5 sliders jumped to its exact `{realYields:0.5, dxy:1.8, inflationFedPath:-0.3}` state and the chart moved accordingly, including LSTM's larger drop (−495 vs. the raw −399 linear sum) correctly reflecting its "amplified when aligned" behavior since all three active drivers pointed the same direction. "Reset to baseline" confirmed clearing all 5 sliders to 0.0, all 4 models back to $4,015, and the trace panel back to its empty-state message.
- Responsive check at 360/768/1920px (Section F.3): 360px is where the chart's own `min-width` forces horizontal scroll inside its card — confirmed the new scroll-hint chevron appears exactly when content is actually cut off and disappears once fully scrolled, confirmed absent at 768px and 1920px where the chart fits without scrolling (no false positive).
- Console: 0 errors in the final, currently-served code, confirmed in a freshly opened tab with a clean console buffer. One error *did* appear during active mid-fix iteration (a React hook-order dev warning, `read_console_messages`' accumulated log made it look like it was still happening on later checks) — traced conclusively to a one-time React Fast Refresh transition between two intermediate versions of `ForecastBandChart`'s hooks, not a defect in the code actually shipped; confirmed by diffing the warning's printed "previous deps" against an already-superseded draft and by re-checking in a brand-new tab with zero interaction, which showed no errors at all. Logged here as a testing-harness lesson: this tool's console buffer accumulates across the whole tab lifetime and is not cleared by navigation, so a stale error from mid-edit can resurface on every later check in the same tab — cross-check suspicious errors against a fresh tab before treating them as live.

---

## Module 7: Macro Economics Lab (nowcasting)

**Built:** Macro as the second domain lab, proving out the "content, not new mechanics" claim from Module 6 while extending the shared infrastructure for two real, concrete needs it hadn't hit yet. `src/data/ml/domains/macro.js` — doc §6.2's information-gap framing and three-model comparison (DFM/Random Forest/LASSO), five illustrative driver instantiations of the doc's named proxy-data categories (yield curve, survey sentiment, labor market, mobility/spending, trade/freight — the doc names categories, not a specific list the way §6.1 does for Gold, so the file's header comment discloses this explicitly), and the "simplicity can win" callout text quoting the doc's actual finding. `computeMacroForecasts()` (added to `domainModel.js`) — three models with genuinely different *jobs*, not just different math shapes: DFM heavily damped (×0.5, "most accurate backcasts"), Random Forest reusing the same tanh-saturation shape as Gold's tree-ensemble lens (a real match — Random Forest *is* a tree ensemble), LASSO given the full undamped linear read rather than rigged to numerically "win" against anything, since the doc's actual simplicity-wins finding is cited in prose, not fabricated as proof inside the toy simulation. `InformationGapTimeline.jsx` — a static SVG diagram (quarter-end → today → official release, with the gap shaded and a "nowcast fills the gap" pin at today) — kept Macro-specific rather than pulled into the shared `components/ml/domain/` folder, since nothing else needs this exact structure yet. `MacroLabPage.jsx` ties it together with the same context/drivers/chart/trace-panel shape as Gold, plus the gap diagram and simplicity callout as Macro's own two domain-specific pieces — matching what Phase 1 predicted this module would need.

**Files touched:** `src/lib/domainModel.js` (added `computeMacroForecasts`), `src/data/ml/domains/macro.js` (new), `src/features/ml/macro/{MacroLabPage,InformationGapTimeline}.jsx` + `.css` (new; `MacroLabPage.jsx` replaces the Module 1 placeholder), `src/lib/useHorizontalScrollHint.js` (new — extracted from Module 6's inline fix), `src/components/ml/domain/ForecastBandChart.jsx` + `.css` (extended, see decisions), `src/components/ml/domain/TracePanel.jsx` (extended, see decisions).

**Decisions (rule 1):**
- **Found and fixed two real formatting bugs in the shared components while wiring Macro's percentage-point-scale data through them** (not decisions made in advance): `ForecastBandChart`'s point-label hardcoded a literal `$` instead of using its own `unit` prop (only the axis labels respected it) — invisible until a second, non-dollar consumer existed. `TracePanel` had the identical hardcoded-prefix issue. Both also rounded to whole numbers, which is fine for Gold's $10s-$100s-scale deltas but collapses Macro's tenths-of-a-percentage-point deltas to indistinguishable integers — and the chart's domain padding was a hardcoded ±20 (dollar-scale), which would have flattened Macro's ~0.5pp-wide bands to an imperceptible sliver. Fixed all three with optional, backward-compatible props (`unitPosition`, `decimals`, `domainPadding`) defaulting to Gold's exact original behavior — rebuilt and re-verified Gold's numbers byte-for-byte unaffected before proceeding, same discipline as Module 3's shared-layout extension. One incidental, disclosed improvement: `TracePanel`'s negative-value format changed from `$-270` to `-$270` (sign before unit) as a side effect of the fix — objectively more conventional, re-verified against Gold's own trace panel rather than assumed safe.
- **Extracted `useHorizontalScrollHint()` into `src/lib/` rather than duplicating Module 6's inline scroll-hint logic**, once `InformationGapTimeline` turned out to need the identical narrow-viewport-overflow affordance as `ForecastBandChart` — a second real consumer, not speculative sharing. While wiring it up, found that a page mounted in a background browser tab has its `ResizeObserver`/`setTimeout` callbacks throttled (confirmed directly: `document.visibilityState` read `"hidden"` during testing, and the hint only appeared once a `resize` event was dispatched or the settle-timer's effects otherwise ran) — added a `visibilitychange` listener so a chart that first laid out while hidden re-measures once the tab is actually seen, rather than staying stuck. This is a real fix for real backgrounded-tab loads, not just a workaround for the test harness.
- LASSO's model note and the "simplicity can win" callout are two separate UI pieces rather than one, so the per-model note cards stay visually consistent with Gold's/Random Forest's/DFM's (uniform card treatment, no visual favoritism baked into the grid), while the *finding* that simplicity sometimes wins gets its own distinct, cited callout — keeps the illustrative simulation honest (no model is dressed up to "look like the winner") while still surfacing the real research result.
- Checked whether Stats mode has a Dynamic Factor Model node to cross-link (the doc calls DFM "your Stats page's home turf") — it doesn't; Bluman's Elementary Statistics doesn't cover DFMs. Left DFM's connection to regression/time-series as prose in its model note rather than a fake or fragile link; real Stats↔ML cross-linking is Module 10's job, not this one's.

**Verification:**
- `npm run build`: pass; `MacroLabPage` in its own ~21KB lazy chunk, `domainLabShared` correctly split into its own shared chunk once a second page imported it alongside Gold's — confirms the "content, not mechanics" sharing claim at the bundler level, not just in source.
- `npm run lint`: pass, 0 warnings.
- Interaction smoke test against the module's done-criterion: moved Yield curve slope to −1.8 → linear delta −0.63pp, DFM 1.8pp (damped ×0.5), Random Forest 1.7pp (tanh saturation), LASSO 1.5pp (full linear) — all three hand-verified against `computeMacroForecasts()`'s formulas exactly, trace panel's −0.6pp matching the raw linear contribution. Applied "Broad-based expansion" (all 5 drivers positive) → DFM 2.8pp, Random Forest 3.2pp, LASSO 3.4pp (largest move, since undamped and every driver aligned) — again hand-verified exactly. "Reset to baseline" confirmed clearing all 5 sliders and both trace panels (Gold's and Macro's, independently) to their empty state.
- Cross-check against the research doc: §6.2's three claims (DFM best backcasts, Random Forest best nowcasts/inflation, LASSO/Elastic Net often outperforms both when data is rich) are reflected in each model's note text and the simplicity callout, sourced via `<MLCitation section="6.2" />`; the base GDP-growth value and all driver coefficients are `synthetic`-labeled since the doc gives no specific nowcast figure to cite the way it does Gold's spot price.
- Responsive check at 360/768/1920px: both `ForecastBandChart` (Macro's own instance) and the new `InformationGapTimeline` correctly overflow and show the scroll-hint chevron at 360px, correctly fit with no hint at 768/1920px — verified by direct `scrollWidth`/`clientWidth` measurement (not just the hint's presence, given the background-tab throttling noted above) at all three breakpoints.
- Bilingual check: Burmese renders cleanly (no tofu boxes) across the context card, gap diagram labels, driver relations, and model notes; mixed English technical terms within Burmese sentences ("Nowcast", "GDP") follow the same established convention as Gold's "Fed"/"DXY" usage rather than being forced into an artificial full translation.
- Console: 0 errors in the final, currently-served code, confirmed in a fresh tab with a clean buffer both before and after the scroll-hint fix. One instance of Module 6's exact same stale-HMR-buffer artifact recurred during active editing (same diagnostic — fresh tab, zero errors — confirmed it again, not re-litigated at length this time since the pattern is now established).

---

## Module 8: Micro Economics Lab (elasticity)

**Built:** A deliberately different shape from Gold/Macro — Micro's headline concept (per ML-Research-Reference.md §6.3) is one continuous price→quantity relationship, not N discrete drivers compared at a single output point, so this module is a bespoke chart plus two model families rather than another `ForecastBandChart` instance. `src/features/ml/micro/demandModel.js` — `logLogQuantity()`: the textbook constant-elasticity power law. `mlQuantity()`: the same curve capped at a realistic saturation ceiling, with a genuine step discontinuity at a $15.00 charm-pricing threshold, and reacting to promotion/season toggles that `logLogQuantity()` structurally cannot see — every one of these three mechanisms exists specifically to demonstrate the doc's stated claim ("ML-based price elasticity models outperform classic log-log regression by incorporating promotions, seasonality, and nonlinear/non-monotonic price-demand relationships that traditional econometrics assumes away"), not as generic embellishment. `demandGapContributions()` — an additive decomposition (saturation → threshold → promo → season) reusing `TracePanel`'s `{key, contribution}` contract. `DemandCurveChart.jsx` — a new bespoke D3-scaled SVG: the log-log curve as one continuous path, the ML curve as two separate path segments (below/above the charm threshold) so the discontinuity renders as a real visual step rather than a smoothed diagonal, both swept across the full price range with a vertical marker + dots at the current price. `MicroLabPage.jsx` — a single price slider (not a 5-key `DriverPanel`, since price is the one genuinely continuous driver here) plus two toggle buttons for promotion/season, reusing `ScenarioPresets`/`TracePanel`/`MLCitation`/`useMLDomainStore` exactly as-is.

**Files touched:** `src/features/ml/micro/{demandModel,DemandCurveChart,MicroLabPage}.jsx` + `.css` (new; `MicroLabPage.jsx` replaces the Module 1 placeholder), `src/data/ml/domains/micro.js` (new).

**Decisions (rule 1):**
- **Did not force Micro's price/promotion/season state through `DriverPanel`**, even though it's the established pattern from Gold/Macro: `DriverPanel`'s rows are built for continuous ~(-2..2) sliders, and modeling "is there a promotion right now" as a 0–1 slider with a 0.1 step would be a worse UI for a naturally binary concept than a plain toggle button. Reused `useMLDomainStore`'s generic `{domain: {key: value}}` shape and `ScenarioPresets`/`TracePanel` as-is (both are genuinely shape-agnostic — neither cares whether keys are driver z-scores or a price/booleans), which is what actually made this affordable to build differently without forking any state management.
- **The ML curve is drawn as two separate `<path>` elements, not one**, specifically so the charm-price effect renders as a discontinuous step. A single path sampled across the threshold would have linearly interpolated through the gap, visually erasing the exact effect (a threshold discontinuity) the module exists to demonstrate.
- **No dollar or percentage figure in this module is doc-cited** — unlike Gold's real spot price or Macro's doc-referenced model comparisons, §6.3 states the *mechanisms* (promotions/seasonality/nonlinearity beat classic log-log) without a specific reference price or elasticity value. `MICRO_REFERENCE`'s numbers are disclosed as illustrative in the file's own header comment, and the context/chart sections cite §6.3 only for the qualitative claim, not for any number on screen.

**Verification:**
- `npm run build`: pass; `MicroLabPage` in its own ~17KB lazy chunk.
- `npm run lint`: pass, 0 warnings (one caught during writing, not after: passed a prop literally named `ref` to a function component, which collides with React's reserved ref handling — `props.ref` doesn't behave like a normal prop even in React 19's simplified model. Renamed to `reference` before it ever ran, rather than discovering it as a runtime bug).
- Interaction smoke test against the module's done-criterion: at the reference price ($14.99, just below the $15.00 threshold) both models read exactly 500 units — hand-verified. Moved price to $17.50: log-log 378, ML 333 (378×0.88 from the charm-price step), trace panel's only row read exactly "−45 units" (378.4×−0.12), matching by hand. Set price to $9.50 with both toggles on: log-log 1,136 (uncapped), ML 1,404; trace panel showed all three active mechanisms (saturation −236, promo +270, season +234) which sum with the base to 1,404.0 exactly, confirming the decomposition and the chart's displayed values are the same computation, not two independently-approximated numbers that happen to look similar. Scenario preset and reset both confirmed setting/clearing price, both toggles, and all three of Gold/Macro/Micro's independent trace panels correctly.
- Responsive check at 360/768/1920px: `DemandCurveChart` (min-width 420px, same shared-hook pattern as Module 7) correctly overflows with a scroll hint at 360px, fits with none at 768/1920px — verified by direct `scrollWidth`/`clientWidth` measurement per the Module 7 procedure for this environment's background-tab timer throttling.
- Bilingual check: Burmese renders cleanly across context, price/toggle labels, scenario presets, and model notes; no tofu boxes.
- Console: 0 errors, confirmed in a fresh tab.

---

## Module 9: Political & Geopolitical Forecasting Lab

**Built:** Two independent mechanisms per §6.4, plus this build's first real, working cross-domain link. `src/features/ml/politics/politicsModel.js` — `computeElection()`: a hierarchical-Bayesian-style estimate where `pollsWeight` shifts from the structural "fundamentals" prior toward the accumulating polls as `daysUntilElection` falls (real practice, not decoration — this is how The Economist's and similar models actually reweight), and the credible-interval half-width shrinks via `sqrt(daysUntilElection/maxDays)`. `computeGeoRisk()`/`geoRiskToGoldZ()`: a 3-signal composite risk score (military/diplomatic/sanctions) mapped onto Gold's `geoRisk` driver's exact −2..2 range. `ProbabilityGauge.jsx`/`GeoRiskMeter.jsx` — lightweight CSS meters (not SVG/D3 charts), following `CompassMeter`'s Module 3 "compact bar, not a heavy chart" precedent, since a single point-plus-band and a single composite score don't need a full coordinate-system chart. `PoliticsLabPage.jsx` reuses `DriverPanel`/`ScenarioPresets`/`TracePanel`/`useMLDomainStore` for both the election drivers (fundamentals/polls) and the geo drivers (military/diplomatic/sanctions), plus a bespoke days-until-election slider (same "one continuous, differently-shaped input" pattern as Micro's price slider). The geo section's "See how this moves the gold price →" button calls `useUIStore.navigateToLinkedConcept('ml', 'gold', {geoRiskValue})` — the store's own comment already anticipated this exact use case. `GoldLabPage.jsx` was extended (not just Politics) with a `useEffect` that consumes an incoming `linkedConcept` addressed to `'gold'`, applies it via the existing `setDriver`, and shows a dismissible banner — the first real consumer of the cross-link infrastructure Module 2 started emitting into (into Module 10's still-unbuilt Bridge tab) back in Module 2.

**Files touched:** `src/features/ml/politics/{politicsModel,ProbabilityGauge,GeoRiskMeter,PoliticsLabPage}.jsx` + `.css` (new; `PoliticsLabPage.jsx` replaces the Module 1 placeholder), `src/data/ml/domains/politics.js` (new), `src/data/ml/domains/gold.js` (added `GOLD_LINK_BANNER`), `src/features/ml/gold/GoldLabPage.jsx` + new `.css` (extended to consume the cross-link).

**Decisions (rule 1):**
- **Caught and fixed a real crash during verification, not just a display bug**: `electionContributions()` originally returned contribution keys `'fundamentals'`/`'polls'`, but `ELECTION_DRIVERS`' actual keys are `'fundamentalsLean'`/`'pollsLean'` — a naming mismatch that made every lookup in `TracePanel`'s `driversByKey` resolve to `undefined`, and `useT(undefined.label)` threw inside `<TraceRow>`. There is no error boundary anywhere in this app, so the throw unmounted the entire tree — a fully blank page, confirmed via console (`An error occurred in the <TraceRow> component`) after a same-script slider change appeared to silently do nothing. Fixed by renaming the contribution keys to match `ELECTION_DRIVERS` exactly, then re-verified the identical interaction end-to-end. Flagging for Module 12: this app has zero error boundaries, so any future single bad prop can white-screen the whole thing — worth a top-level `ErrorBoundary` as a hardening pass, but that's cross-cutting infrastructure touching both modes, out of scope for a single domain module's diff.
- **Scenario presets specify a full 6-field snapshot (election *and* geo fields together)**, not two separate partial lists, because `applyScenario()` fully replaces a domain's state rather than merging — an election-only preset that omitted the geo fields would have silently reset military/diplomatic/sanctions to their `?? 0` fallback on every click, which is surprising behavior a learner wouldn't expect from a button labeled "Toss-up race." Verified this deliberately: applying "Regional conflict escalates" after previously setting `daysUntilElection` elsewhere correctly reset it to that scenario's own specified value, not a stale leftover.
- `GOLD_LINK_BANNER`'s text lives in `gold.js`, not `politics.js`, even though Politics triggers it — it's Gold's own UI copy for Gold's own banner; Politics shouldn't own text that isn't its page's.
- Checked whether Stats mode has literal "Bayesian" or "Gibbs sampling" content before writing the cross-reference the doc suggests ("architecturally very close to your existing Bayesian/Gibbs-sampling Stats content") — it doesn't by that name, but Chapter 14: Monte Carlo genuinely exists (`src/data/chapters.js`, node ids `mc_prob`/`mc_steps`) and is the real simulation-based-uncertainty content the doc means. Referenced that by name in prose; did not build a live cross-link into the Stats Map component for it, since Map is explicitly flagged elsewhere as something not to touch beyond what's needed, and the mission's own explicit cross-link requirement for this module is specifically the geoRisk→Gold one, which is built and live.

**Verification:**
- `npm run build`: pass; `PoliticsLabPage` in its own ~18KB lazy chunk.
- `npm run lint`: pass, 0 warnings.
- Interaction smoke test against the module's done-criterion: at baseline (90 days out), gauge read exactly "50% ± 16pts" (`3+(22-3)×√(90/180)=16.44`, hand-verified). Days slider to 5 → band narrowed to "±6pts" (`3+19×√(5/180)=6.17`, matches). With days=5, polls lean set to +2 → "86% ± 6pts" and trace panel read "+1.94" for polling average, both matching `computeElection()`/`electionContributions()` by hand exactly. Geo sliders (military 1.8, diplomatic 1.2, sanctions 0.8) → composite score "99/100" (`50+1.8×15+1.2×12+0.8×10=99.4`, matches) — clicking the cross-link button navigated to the Gold tab, showed the dismissible banner, set Gold's geoRisk driver to "+2.0" (`(99.4-50)/25=1.976`), and all four Gold models reacted with values matching `computeModelForecasts()` exactly (ARIMA $4,167, LSTM $4,232, XGBoost $4,130, GARCH $4,167) — a full, hand-verified round trip through two domains' worth of math, not just a navigation check. Returning to Politics confirmed its own state (both election and geo sliders) survived the round trip untouched. Reset (correctly scoped to Politics' own reset button — an earlier check had grabbed Gold's by mistake via an unscoped `querySelector`, a test-script error rather than an app bug) cleared both sections back to baseline together, confirming they share one domain state as designed.
- Responsive check at 360/1920px: both CSS meters (no SVG min-width constraint, unlike the other three domain labs) reflow cleanly with no overflow at either size.
- Bilingual check: Burmese renders cleanly across both sections, including the gauge's Challenger/Incumbent labels and the cross-link button — confirmed via direct textContent read, not just visual inspection, after the compressed screenshot made the gauge's flanking labels hard to eyeball.
- Console: 0 errors in the final, currently-served code, confirmed in a fresh tab both before and after the TraceRow crash fix.

---

## Module 10: Stats ↔ ML Bridge

**Built:** Two shared-example comparisons, both reachable from both modes, per the mission's explicit requirement. `src/data/ml/bridge.js` — a regression comparison (8-student study-hours-vs-score example: Stats lens reads r²/sₑ/prediction-interval as in-sample fit statistics about a fixed population parameter; ML lens reads the *same* fitted coefficient as a learned weight judged by held-out test RMSE) and an estimation-philosophy comparison (40-commuter average: a frequentist 95% CI vs. a Bayesian 95% credible interval that are numerically identical under a weak prior — deliberately, since the real difference is what the interval *means*, not what it looks like). `BridgePage.jsx` renders both as side-by-side cards, with clickable buttons to five real Stats-mode nodes (`reg`/`pearson`/`r2`/`se_est`/`pred_int`) and two more (`x_bar`/`ci_mean_t`), plus a cross-link to the Politics lab's genuinely-Bayesian election model. Two new consumers of the existing `linkedConcept` cross-mode infrastructure: `MapPage.jsx` (Stats side) now accepts an incoming `{nodeId}` payload and selects that node; `DetailPanel.jsx` gained a "Compare this to the ML lens" button, shown only on the `reg` node, completing the round trip Module 2's Estimation demo started.

**Files touched:** `src/data/ml/bridge.js` (new), `src/features/ml/bridge/BridgePage.jsx` + `.css` (new; replaces the Module 1 placeholder), `src/features/map/MapPage.jsx` (extended to consume `linkedConcept`), `src/components/detail/DetailPanel.jsx` + `.css` (extended with the `reg`-only cross-link button), `src/features/ml/pipeline/EstimationDemo.jsx`, `src/data/ml/estimationPredictionCausal.js`, `src/data/ml/models.js` (all three: see decisions).

**Decisions (rule 1):**
- **The research doc's §7.5 claim — "your existing Stats/Gibbs/PGAS content" as the natural Bayesian bridge — is false**, and this was caught by actually checking rather than trusting the doc at face value: `src/data/nodes.js` has no Gibbs sampling or PGAS content anywhere. What Stats mode genuinely has is Chapter 7's t-distribution confidence interval for the mean (`ci_mean_t`), which is what this module's second comparison and all cross-links actually use. **Fixed the same false claim in three places already shipped from earlier modules** (found while grepping for what "Gibbs/PGAS" content actually existed, not something this module set out to audit): `EstimationDemo.jsx`'s displayed paragraph (Module 2), `estimationPredictionCausal.js`'s "Method family" cell (Module 2), and `models.js`'s BSTS/Bayesian-NN `usageAreas` (Module 3) — all three told a learner Stats mode had content it doesn't. Left `models.js`'s `howItWorks` field alone, since its Gibbs-sampling mention there is accurate ML knowledge (a real BSTS estimation technique), not a Stats-mode claim.
- **Caught and fixed a second real bug during verification**: `clearLinkedConcept()` was originally called synchronously inside the effect that scheduled the scroll-to-comparison timer. Since `linkedConcept` is that same effect's own dependency, the synchronous call triggered an immediate re-render whose cleanup canceled the timer before it ever fired — confirmed by direct instrumentation (a temporary `window.__bridgeDebug` trace log), not guessed. Fixed by moving `clearLinkedConcept()` inside the timeout callback, so the effect doesn't invalidate its own dependency mid-flight.
- **`scrollIntoView` uses `behavior: 'auto'`, not `'smooth'`** — also found via the same instrumentation: the call was firing correctly and on a valid, attached DOM node, but the frame-by-frame animation a smooth scroll depends on (`requestAnimationFrame`) never visibly progressed in this testing environment. An instant jump has no such dependency and suits a "take me there now" button regardless of environment.
- All worked-example numbers (8 students, 40 commuters) are disclosed as illustrative in `bridge.js`'s own header comment — the doc supports the *mechanisms* (in-sample vs. out-of-sample evaluation; frequentist vs. Bayesian interval interpretation) without a specific reference study for either.

**Verification:**
- `npm run build`: pass; `BridgePage` in its own ~18KB lazy chunk.
- `npm run lint`: pass, 0 warnings (one caught while writing, not after: `points` fields were originally arrays passed into `bl()`, but `useT()` returns whatever type was stored and the component called `.split('\n')` on the result — arrays have no `.split`, so this would have thrown immediately on render. Fixed before it ever ran by joining each array into a string first.)
- Interaction smoke test against the module's done-criterion (reachable from both modes, real content underneath): clicked all 5 regression node-links and both estimation node-links from the Bridge — each correctly switched to Stats mode, landed on the Map tab, and selected the exact right node with its real formula/description/inventor info. From the `reg` node's own detail panel, the new "Compare this to the ML lens" button correctly returned to the Bridge, auto-scrolled to the regression comparison. From Module 2's Estimation tab, "See this compared side-by-side" correctly landed on the Bridge, auto-scrolled to the estimation-philosophy comparison — confirmed by direct `getBoundingClientRect()` measurement at a viewport short enough to make the scroll distance unambiguous (539px of a 539px-max scroll range), not just eyeballed. The Politics cross-link correctly landed on the Politics lab.
- **Found and fixed a real crash during this verification, not a display bug**: the `clearLinkedConcept`-timing bug above didn't just fail silently — before the fix, it also fully white-screened once mid-testing on an unrelated earlier bug in the same session (the array/`.split()` issue), confirming again (as in Module 9) that this app's total absence of error boundaries makes any single bad prop or bad effect fatal to the whole page, not just the offending component. Same flag for Module 12 as Module 9's: worth a top-level `ErrorBoundary`, out of scope for this module's own diff.
- Responsive check at 360/1920px: both comparisons and all node-link rows reflow cleanly with no overflow at either size (no SVG chart in this module, so no scroll-hint concern).
- Bilingual check: Burmese renders cleanly across both comparisons, all interpretations, and the cross-link buttons; no tofu boxes.
- Console: 0 errors in the final, currently-served code, confirmed in a fresh tab.

---

## Module 11: Beginner/Researcher + EN/Burmese cross-cutting verification pass

**Built:** Not a new feature — the mission's own required cross-cutting audit of every module 2-10 against the "full bilingual, Beginner/Researcher depth toggle applying to every module" requirement, and a genuine, systemic finding, not a clean pass. Body content (driver descriptions, model notes, scenario text, interpretations) had been correctly wrapped in `bl()`/`useT()` since Module 2. Page *chrome* had not: section titles (`ml-section-title`), field labels (`ml-lbl`), buttons, chart legends, and SVG axis/annotation text across nearly every ML component were passed into JSX as raw English string literals, invisible to the language toggle. Root cause: the pattern was never wrong in any single module, it was simply never applied to chrome text, and each new module copied the same omission from the last one. Fixed file-by-file across all nine modules 2-10 (Pipeline, Model Map, Playground, Evaluation, Gold, Macro, Micro, Politics, Bridge): every hardcoded string got a `blSame(en, my)` (or `bl()` where beginner/researcher wording genuinely differs) constant added to its module's data file, resolved via `useT()` in the component, with dynamic values (e.g. "Noise level: 0.8", "origin t=8 — …") kept as separate interpolated variables rather than re-templated per value. During the final verification pass (browser-toggling Playground and Evaluation end-to-end), also found `MLCitation.jsx` — the citation footer mounted on nearly every page in every module — had both of its own variants ("Illustrative data, calibrated to real-world patterns" and "Source: ML-Research-Reference.md §N") hardcoded, which the file-by-file sweep had missed because it lives in `components/ml/`, not inside any one module's own tree. Fixed the same way, using the shared `src/data/ml/uiStrings.js` (a Module 11 addition from earlier in this same pass, for strings repeated verbatim across modules) since this component is more widely shared than anything already in that file.

**Files touched:** `src/data/ml/playground.js`, `src/features/ml/playground/{PlaygroundPage,ScatterFitChart,ErrorCurveChart}.jsx` (Playground); `src/data/ml/evaluation.js`, `src/features/ml/evaluation/{MetricsPanel,BacktestAnimator,ShapLimePanel,ConceptDriftDemo}.jsx` (Evaluation); `src/data/ml/uiStrings.js`, `src/components/ml/MLCitation.jsx` (shared citation, all modules); plus, earlier in this same pass, the equivalent fix already applied and committed against Pipeline, Model Map, Gold, Macro, Micro, Politics, and Bridge's own page/component/data files.

**Decisions (rule 1):**
- **Dynamic-value labels split the static label from the interpolated value** (`{noiseLbl}: {noise.toFixed(1)}`) rather than storing a pre-formatted string per possible value — consistent with how earlier modules (e.g. Politics' days-until-election) already handled this, and the only approach that doesn't require enumerating every possible numeric value.
- **`BacktestAnimator`'s walk-forward sublabel is the one exception**: it embeds two values (`origin`, step count) *inside* a sentence rather than at a fixed prefix, and Burmese word order doesn't match English closely enough to safely concatenate translated fragments around them. Stored as one full-sentence `bl()` template per language with literal `{origin}`/`{steps}` tokens, resolved via `useT()` then `.replace()`'d — verified in-browser that both languages substitute correctly (`origin t=8 — origin အထိ train လုပ်ပြီး နောက် 4 steps ကို test လုပ်သည်…`).
- **Short ML/stats terms-of-art stay English loanwords in the Burmese variant** (train/test/MSE, Underfitting/Overfitting, Polynomial/Decision Tree) — matching the convention the body-text `bl()` entries already established throughout every earlier module, not a new inconsistency introduced here.
- **`aria-label` attributes were left untranslated**, consistent with existing precedent elsewhere in the app (e.g. `ConfounderDemo`'s SVG `aria-label` from Module 2) — screen-reader-only text was never in scope for the *visible* bilingual requirement, and treating it differently now would be an unrelated scope expansion.
- **Did not attempt to fix the app-wide missing-error-boundary issue** flagged in Modules 9 and 10 — still cross-cutting infrastructure beyond a verification pass, left for Module 12 to decide on.

**Verification:**
- `npm run build`: pass. `npm run lint`: pass, 0 warnings.
- Full-tree grep sweep across `features/ml`, `components/ml`, `components/mlgraph` for three hardcoded-text shapes — literal text directly inside `ml-section-title`/`ml-lbl`/`ml-section-sub`, any capitalized JSX text node, any lowercase-starting JSX text node, and hardcoded `placeholder`/`title` attributes — all five passes returned zero matches after the fixes, confirming no further chrome strings were missed by the file-by-file pass.
- Browser-verified the EN↔Burmese toggle end-to-end on both newly-fixed modules: Playground (title, model/noise/degree labels, New sample button, fit legend, both chart legends, zone banner, train/validation MSE line) and Evaluation (all four sub-panels: Metrics' model buttons and complexity label, Backtest's naive/walk-forward labels including the two-value sentence template, Play/Pause/Reset, train/test/unused legend, SHAP/LIME titles and base-value/prediction labels, Drift's threshold/regime/progress/status text) — every string genuinely switches, none were compile-only.
- Re-verified Model Map (a Module 3 page, already shipped before this pass) after the `MLCitation` fix to confirm the shared-component change didn't regress an earlier module — its citation footer ("Source: ML-Research-Reference.md §3") now also correctly translates, with no change to any of that page's own already-correct bilingual content.
- Console: 0 errors across every page checked, in a fresh tab.

**Addendum (found while writing Module 12's `docs/ML_MODE_README.md`, same module — not a new one):** Grounding that doc meant reading every shared component's actual source, which surfaced three more hardcoded strings the sweep above had missed, all for the same reason: the sweep's regex capped matched text at 60 characters, and/or excluded files under `components/layout/`, which was never in the sweep's search path.
- **`TracePanel.jsx`'s empty-state message** ("All drivers are at baseline — move one above to see its effect traced here.", 77 characters) — shown on every domain lab (Gold/Macro/Micro/Politics) before any driver is touched, i.e. the first thing a learner sees on each of those four pages. Longer than the sweep's 60-char cap, so it never matched. Fixed via a new `UI_TRACE_EMPTY` in `uiStrings.js`, same shared-string reasoning as `MLCitation`.
- **`AppShell.jsx`'s ML-mode banner subtitle** ("Estimation, prediction & real-world forecasting — machine learning mode") — rendered in the top header regardless of which ML page is open, so it's arguably the single most-visible unfixed string once found. `components/layout/` was outside every grep path used in this module's verification, Stats/ML shared shell having been treated as Module 1's territory rather than re-swept here. Fixed in place (a local `blSame()`, not a data-file constant, since nothing else consumes it); Stats mode's own subtitle was deliberately left as a plain string — it predates and sits outside the ML bilingual mandate entirely.
- **`DemandCurveChart.jsx`'s SVG y-axis title** ("units/week") — a bespoke chart's own internal text, same blind spot as Playground's `ScatterFitChart`/`ErrorCurveChart` in the original pass. Fixed via a new `MICRO_UNITS_LABEL` in `micro.js`. (Its "Log-Log Regression (constant elasticity):" / "Gradient Boosting:" legend prefixes were checked and are *not* a bug — `MICRO_MODELS`' `name` fields are legitimately `blSame()`-English, the same "model names stay English loanwords" convention as ARIMA/LSTM/XGBoost elsewhere.)
- Re-ran the sweep with the length cap removed and `components/layout/` included; the only remaining hits were the app's own brand name ("Formula Intelligence," correctly untranslated) and `NavSidebar.jsx` (Stats mode's sidebar, out of scope). `npm run build`/`npm run lint`: pass. Browser-verified all three fixes directly (`unit/အပတ်`, the translated trace-empty sentence, and the translated banner subtitle all render correctly in Burmese and correctly revert to English), plus a full `read_page` dump across every keep-alive-mounted ML page at once, spot-checked against the two languages — 0 console errors.

---

## Module 12: Final verification, docs, extensibility note

**Built:** The three end-of-build deliverables the mission's own build order calls for, plus a final full-app verification gate. `docs/DATA_SOURCES.md` — a module-by-module accounting of every real (doc-cited, with §-reference) vs. synthetic (illustrative, disclosed) figure across all nine ML tabs, plus a dedicated record of the one confirmed inaccuracy in the research document itself (§3.9/§7's false claim that Stats mode has existing Gibbs/PGAS content — verified false by grep in Module 10, corrected in three shipped files, doc itself left as-is). `docs/ML_MODE_README.md` — an architecture map of all nine modules, the bilingual/depth system's rules (using Module 11's own gap as the worked cautionary example for why every string must be wrapped the first time, not retrofitted), and a step-by-step "add a 13th domain lab" template naming every shared piece (`useMLDomainStore`, `DriverPanel`, `ScenarioPresets`, `ForecastBandChart`/`TracePanel`, `domainModel.js`'s compute functions, `MLCitation`, the `linkedConcept` cross-link pattern) plus explicit guidance on when to deviate from the standard shape (Micro's continuous driver, Politics' two-sub-mechanism page, Bridge's non-domain-lab shape) — written to close the loop `domainModel.js`'s own header comment already promised ("see docs/ML_MODE_README.md"). `BUILD_SUMMARY.md` — the end-of-build report: what was built, the module table, the architecture patterns established, the verification discipline followed throughout, and both known limitations carried forward. Also added a short "ML mode" section to the top-level `README.md`, which previously didn't mention ML mode's existence at all — a real gap for anyone landing on the repo fresh, not just a documentation nicety.

**Files touched:** `docs/DATA_SOURCES.md` (new), `docs/ML_MODE_README.md` (new), `BUILD_SUMMARY.md` (new), `README.md` (added ML mode section), `BUILD_LOG.md` (this entry).

**Decisions (rule 1):**
- **Grounded every claim in the two new docs against the actual current source**, not against memory of what was built — re-read `docs/research/ML-Research-Reference.md` in full before writing `DATA_SOURCES.md` rather than reconstructing its section numbers from earlier BUILD_LOG entries, and re-read `GoldLabPage.jsx`, `domainModel.js`, `useMLDomainStore.js`, `useMLUIStore.js`, `useUIStore.js`, `DriverPanel.jsx`, `ScenarioPresets.jsx`, `TracePanel.jsx`, and `MLBody.jsx` directly before writing the extension template in `ML_MODE_README.md`, so every file path, prop name, and function signature named in either doc is verified against the real, current code rather than asserted from summary.
- **The error-boundary gap is flagged a fourth time (Modules 9, 10, 11, now 12) and still not fixed** — reaffirming the same reasoning each time: it's shell-level infrastructure equally relevant to Stats and ML mode, not a fit for any single module's diff, including this closing one. Documented with enough specificity (`AppShell` as the likely wrap point) that picking it up doesn't require re-deriving the diagnosis.
- **Did not attempt code-splitting** despite the build's own repeated warning about the main `index-*.js` chunk exceeding 500kB — out of scope for a docs-and-verification module, and orthogonal to everything this build actually set out to do (a bundling change, not a content or correctness one).

**Verification:**
- `npm run build`: pass, 0 errors (the persistent >500kB main-chunk warning is pre-existing and informational, not a new regression from this module's changes, which are documentation-only).
- `npm run lint`: pass, 0 warnings.
- Confirmed no stray references: every file path and function name cited in both new docs was checked against the actual filesystem/grep output during writing, not assumed.
- Full deliverables check against the mission's own end-of-build requirements: `docs/DATA_SOURCES.md` ✓, `docs/ML_MODE_README.md` ✓, `BUILD_LOG.md` with one entry per module 1–12 ✓ (this file), `BUILD_SUMMARY.md` ✓, 12-module build order followed with one commit per module throughout ✓, full bilingual + Beginner/Researcher coverage across modules 2–10 ✓ (Module 11 + its addendum), all data static/synthetic-and-labeled with zero live API calls ✓ (confirmed directly in `DATA_SOURCES.md`'s own writing pass — grepped for `fetch`/`axios`/any network call anywhere under `src/data/ml` and `src/features/ml`, found none).

---

# Learning Design System Build (new mission)

A new, more detailed mission was received after the above 12 modules shipped: `docs/research/ML-Mode-Pedagogy-Research.md` specifies a full pedagogical framework — the Depth Ladder (4 layers per node), the Constructive Thought Loop (Predict→Explore→Compare→Explain→Retrieve on every interaction), a Named Misconceptions Registry (6 specific refutations in specific modules), Mixed-Review interleaving, and an Understanding Tracker (Dreyfus 5-stage vocabulary) — that is not present anywhere in the build above. Everything shipped so far is real, working *coverage*; this phase adds the *teaching pattern* the mission is explicit is not optional ("Breadth without this pattern is a brochure"). Treating this as a continuation, not a restart — Modules 1–10's content, the bilingual system, and all of Module 11/12's fixes stay as-is and get built on top of, not replaced.

## Phase 0: Discovery

Re-verified fresh rather than assumed from the prior build:
- **Stack**: React 19.2.7 + Vite 8.1.1, Zustand 5.0.14, D3 7.9.0, Framer Motion 12.42.2 (installed but not yet used anywhere in ML mode — a real, already-available option for the Predict→Compare reveal animation rather than a new dependency). `package.json` scripts: `dev`/`build`/`preview` (Vite), `lint` (oxlint). No typecheck script — this is a plain-JS app (`@types/react` present for editor support only), so `npm run build` (Vite's own type-agnostic bundling) + `npm run lint` are the full verification pair, matching what every module so far has used.
- **Existing bilingual/i18n system** (`src/lib/mlContent.js`): `bl(enBeginner, enResearcher, myBeginner, myResearcher)` / `blSame(en, my)` / `useT()`, keyed off `useMLUIStore`'s `level`/`lang`. Reused as-is for every new string this phase adds — no parallel system needed.
- **Existing progress-tracking precedent** (Stats mode's `useMasteryStore`, `src/store/useMasteryStore.js`): a `localStorage`-persisted (via `STORAGE_KEYS` + `loadJSON`/`saveJSON`) state-machine driven by actual recall performance, not time-on-page. The Understanding Tracker follows this exact persistence pattern (new `STORAGE_KEYS.mlUnderstanding` entry) but with its own vocabulary (Dreyfus stages) and its own trigger (Depth-Ladder layers engaged per node), since the mission specifies Dreyfus stages explicitly and mastery-store's streak/Feynman-score model doesn't map onto that.
- **Model Map's current node schema** (`src/data/ml/models.js`, `ML_MODELS`, 32 nodes / 549 lines): `{ id, ch, name, short, howItWorks: bl(), advantages/weaknesses/usageAreas: blSame(), compass: {interpretability, dataHunger, nonlinearity} }`. No Depth-Ladder-shaped fields exist anywhere — confirms this really is greenfield within the existing content, not a rename of something already there.
- **No existing "predict before reveal" interaction pattern anywhere in ML mode** — confirmed by the extensive browser verification already performed in the prior build phase (every slider/preset redraws immediately on change, no gating step). This is the actual net-new interaction primitive this phase must build.

## Phase 1: Architecture

- **Depth Ladder content lives in the data files**, extending (not replacing) each node/stage/driver's existing object: new `spark: { analogy: bl(), predict: { question: bl(), options: [bl(), ...], correctIndex } }`, `mechanism: { kind: 'live-link' | 'mini-widget', ... }`, `formalism: { worked: bl(), faded: bl() }`, `criticalFrontier: { misconceptionId, analogyBreakdown: bl(), caveat: bl(), retrieval: { question: bl(), answer: bl() } }` fields, resolved via the same `useT()` every other string already uses.
- **`mechanism.kind: 'live-link'` for any model that already has a real, working interactive counterpart elsewhere in ML mode** (Linear/Ridge-Lasso/Decision-Tree/Random-Forest → Playground's live fit; ARIMA/LSTM/XGBoost/GARCH/DFM/LASSO → Gold's or Macro's live model-comparison chart) — a short framing text plus a real `navigateToLinkedConcept` cross-link button into that already-built interactive surface, using Module 9/10's existing cross-link infrastructure. This is a deliberate choice over building 32 independent bespoke widgets: it's more honest (no fabricated redundant toy standing in for a chart that already exists and already works), it materially strengthens the cross-module connections the mission repeatedly asks for, and it avoids sinking the whole engineering budget into duplicate visualizations for models Playground/Gold/Macro already demonstrate live.
- **`mechanism.kind: 'mini-widget'` for every model with no existing live counterpart** (KNN, Naive Bayes, SVM, K-Means, Hierarchical Clustering, PCA, Prophet, RNN/CNN/Transformer, Gaussian Processes, Bayesian NNs/BSTS, Q-Learning/Actor-Critic, etc.) — served by one new generic, parameterized component (`MechanismWidget`, Section below) rather than a bespoke chart per model, so the engineering cost is O(1) component + O(n) parameter objects, not O(n) components.
- **One new shared component, `PredictGate`**, is the actual implementation of the Constructive Thought Loop (D.2) and is used everywhere the mission requires it: Model Map's `MechanismWidget`, every domain-lab driver slider (first interaction per driver per session, not re-asked on every drag — "before any slider ... reveals its result" is read as gating the first encounter, with the Compare result then staying visible as a small persistent badge on that control; re-testing the same judgment later is what Mixed-Review/D.4 is for, not repeated friction on every pixel of drag), the Pipeline's three-bucket sort, and Playground's try-first gate.
- **The Understanding Tracker is its own store** (`useUnderstandingStore`), recording, per node id, which Depth-Ladder layers have been opened/completed; derives a single mode-wide Dreyfus stage from aggregate coverage; exposes a `jumpToAdvanced` affordance (skip Layers 1–2) once a learner is past Competent. Lives in the ML header per the mission, next to the existing Beginner/Researcher and EN/Burmese toggles.
- **Named misconceptions are a small central registry** (`MISCONCEPTIONS` in a new `src/data/ml/misconceptions.js`, one entry per of the 6 named + the unlabeled "less biased than humans" one), each with full bl() refutation text; individual modules reference an id and render a shared `MisconceptionCallout` component, rather than six independently-authored inline blocks that could drift from each other in tone/quality.
- **Mixed-Review** is a small store (`sessionDomainsVisited`, part of `useUnderstandingStore` or a sibling) plus one `MixedReviewPanel` component surfaced in the ML shell once 2+ of Gold/Macro/Micro/Politics have been opened in the session, pulling from a small static bank of cross-domain questions.

Building the shared components first (this entry's own commit), then retrofitting module by module in the mission's Section E order, each its own commit, each logged the same Built/Files touched/Decisions/Verification way as every module above.

---
